import 'package:flutter/material.dart';

class UserStoreScreen extends StatelessWidget {
  const UserStoreScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('User personal store screen'),
    );
  }
}
